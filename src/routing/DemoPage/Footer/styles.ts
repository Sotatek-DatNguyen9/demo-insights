import { makeStyles } from "@material-ui/core";

const styles = makeStyles((theme: any) => {
  return {
    footerWrap: {
      width: "100%",
      height: "100%",
    },
  };
});
export default styles;
